services:
  db:
    image: postgres:15
    container_name: ledgerpulse
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin 
      POSTGRES_DB: ledgerpulse
    ports:
      - "5432:5432"

